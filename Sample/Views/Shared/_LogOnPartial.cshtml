@if(Request.IsAuthenticated) {
    <text>@R("Welcome") <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink(R("Log Off"), "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink(R("Log On"), "LogOn", "Account") ]
}
